{
  "name": "cloud-itonami-kadou",
  "private": true,
  "type": "module",
  "comment": "Edge build: shadow-cljs compiles src/kadou/edge/endpoints.cljk's :cljs branch into functions/edge/kadou-edge-core.js, re-exported by the thin functions/api/*.js shim.",
  "scripts": {
    "build-edge": "amu compile --target wasm32-browser edge-api",
    "deploy": "wrangler pages deploy public --project-name=cloud-itonami-kadou"
  },
  "devDependencies": {
    "shadow-cljs": "^2.28.0"
  }
}
